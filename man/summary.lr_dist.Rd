% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_dist_s3.R
\name{summary.lr_dist}
\alias{summary.lr_dist}
\title{Summarise a Likelihood-Ratio Distribution}
\usage{
\method{summary}{lr_dist}(object, ...)
}
\arguments{
\item{object}{An \code{lr_dist} object (see \code{\link[=as_lr_dist]{as_lr_dist()}}).}

\item{...}{Unused.}
}
\value{
A list of class \code{summary.lr_dist} with components \code{mean_h1},
\code{mean_h2}, \code{var_h1}, \code{var_h2}, \code{sd_h1}, \code{sd_h2}, \code{mass_h1},
\code{mass_h2}, \code{auc}, \code{quantiles_h1}, \code{quantiles_h2}, \code{has_pos_inf}
and \code{has_neg_inf}. Printed in a compact table.
}
\description{
Computes the decision-theoretic summary of an \code{lr_dist} object: the
mean, variance and standard deviation of \eqn{\log_{10}\mathrm{LR}}
under each hypothesis, the median and quartiles, the area under the
ROC curve, and the total probability mass (a sanity check on the
input joint).
}
\details{
\code{mean_h1} is \eqn{E[\log_{10}\mathrm{LR}\mid H_1]} and equals the
per-marker KL-derived expectation. A \code{+Inf} atom drives the H1
moments to \code{+Inf} (and a \code{-Inf} atom the H2 moments to \code{-Inf}),
matching the engine's \verb{0\\log 0 = 0} convention. \code{auc} is the exact
concordance statistic \eqn{P(\mathrm{LR}_{H_1} > \mathrm{LR}_{H_2})
+ \tfrac12 P(\mathrm{LR}_{H_1} = \mathrm{LR}_{H_2})}.
}
\examples{
d <- as_lr_dist(data.frame(
  log10_lr = c(-1, 0, 2),
  p_h1 = c(0.1, 0.3, 0.6),
  p_h2 = c(0.6, 0.3, 0.1)
))
s <- summary(d)
s$mean_h1
s$auc
}
\seealso{
\code{\link[=plot.lr_dist]{plot.lr_dist()}}, \code{\link[=as_lr_dist]{as_lr_dist()}}
}
