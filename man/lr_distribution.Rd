% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_distribution.R
\name{lr_distribution}
\alias{lr_distribution}
\title{Composed likelihood-ratio distribution over a marker profile}
\usage{
lr_distribution(
  models,
  poi = NULL,
  method = c("exact", "grid"),
  grid_points = 512L
)
}
\arguments{
\item{models}{A \code{marker_model} object or a \code{list} of them
(see \code{\link[=marker_model]{marker_model()}}). A single model is accepted and returns its
own per-marker distribution unchanged. List names, when present and
non-empty, are recorded in the \code{"markers"} attribute.}

\item{poi}{Optional character scalar naming the person of interest,
applied to every model. When \code{NULL} (default) the POI is resolved
per model by the joint engine (see \code{\link[=per_marker_kl]{per_marker_kl()}}).}

\item{method}{Composition method. \code{"exact"} (default) performs the
exact sparse convolution and is bit-for-bit with the reference
engine. \code{"grid"} projects each distribution onto a regular lattice
before convolving (mass- and mean-preserving, with a shape error
that shrinks with \code{grid_points}); it requires finite support and is
useful for very long profiles.}

\item{grid_points}{Integer number of lattice points used when
\code{method = "grid"} (ignored for \code{"exact"}). Default \code{512}.}
}
\value{
An object of class \code{lr_dist} (a data.frame with columns
\code{log10_lr}, \code{p_h1}, \code{p_h2}), with attributes \code{"markers"} (the
marker identifiers in input order), \code{"n_markers"}, \code{"method"} and
\code{"poi"} (the resolved POI; a vector if it differs across models).
}
\description{
Builds the exact distribution of the total \eqn{\log_{10}\mathrm{LR}}
for a profile of independent markers. Each marker's per-marker LR
distribution is computed from its joint H1 / H2 table, and the
distributions are convolved (conditional independence: the total
\eqn{\log_{10}\mathrm{LR}} is the sum of the per-marker
\eqn{\log_{10}\mathrm{LR}}s). The result is an \code{\link[=as_lr_dist]{as_lr_dist()}} object,
so \code{\link[=summary.lr_dist]{summary.lr_dist()}}, \code{\link[=plot.lr_dist]{plot.lr_dist()}} and \code{\link[=quantile.lr_dist]{quantile.lr_dist()}}
apply directly.
}
\details{
Markers are assumed conditionally independent given the hypothesis
(no linkage); linked-marker composition arrives with the F5 linkage
work. \code{+Inf} / \code{-Inf} atoms (which arise under \code{mutation = "none"}
when a hypothesis assigns zero mass to a state the other supports)
propagate through the exact convolution and are reported by
\code{\link[=summary.lr_dist]{summary.lr_dist()}}; \code{method = "grid"} rejects infinite support.

Two atoms carry the same \code{log10} LR when they agree to a relative
tolerance of 1e-12, rather than bit for bit. A single atom is a real
number that the engine can reach by more than one arithmetic route, and
on a platform that contracts \code{a * b + c} into a fused multiply-add the
routes differ in the last bits; grouping by exact equality would make the
size of the support depend on the compiler. The tolerance sits four
orders of magnitude above that rounding noise and four below the closest
genuinely distinct pair observed, so it merges duplicates and nothing
else.
}
\examples{
if (requireNamespace("pedtools", quietly = TRUE)) {
  ped <- pedtools::nuclearPed(1)
  models <- list(
    D3 = marker_model(ped, "D3", c("15" = 0.4, "16" = 0.6),
                      mutation = list(model = "equal", rate = 1e-3)),
    vWA = marker_model(ped, "vWA", c("a" = 0.2, "b" = 0.3, "c" = 0.5),
                       mutation = list(model = "equal", rate = 1e-3))
  )
  d <- lr_distribution(models)
  summary(d)
  quantile(d)
}
}
\seealso{
\code{\link[=marker_model]{marker_model()}}, \code{\link[=as_lr_dist]{as_lr_dist()}}, \code{\link[=summary.lr_dist]{summary.lr_dist()}},
\code{\link[=plot.lr_dist]{plot.lr_dist()}}, \code{\link[=quantile.lr_dist]{quantile.lr_dist()}}, \code{\link[=per_marker_kl_profile]{per_marker_kl_profile()}}.
}
