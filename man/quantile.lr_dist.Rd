% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_distribution.R
\name{quantile.lr_dist}
\alias{quantile.lr_dist}
\title{Quantiles of a likelihood-ratio distribution}
\usage{
\method{quantile}{lr_dist}(x, probs = c(0, 0.25, 0.5, 0.75, 1), under_h1 = TRUE, ...)
}
\arguments{
\item{x}{An \code{lr_dist} object (see \code{\link[=as_lr_dist]{as_lr_dist()}} / \code{\link[=lr_distribution]{lr_distribution()}}).}

\item{probs}{Numeric vector of probabilities in \eqn{[0, 1]}.
Defaults to the quartiles \code{c(0, 0.25, 0.5, 0.75, 1)}.}

\item{under_h1}{Logical. If \code{TRUE} (default) quantiles are taken
under \eqn{H_1} (weights \code{p_h1}); if \code{FALSE}, under \eqn{H_2}
(weights \code{p_h2}).}

\item{...}{Unused.}
}
\value{
A named numeric vector of quantiles, one per \code{probs} entry.
}
\description{
Discrete inverse-CDF quantiles of \eqn{\log_{10}\mathrm{LR}} under
one of the two hypotheses, using R's \code{quantile} type-1 definition
\eqn{Q(p) = \inf\{x : F(x) \ge p\}} over the active support.
}
\details{
Atoms with zero mass under the requested hypothesis are dropped
before the cumulative distribution is formed. A \code{+Inf} (or \code{-Inf})
atom is a legitimate quantile value when the requested probability
falls in its mass. An error is raised if no atom has positive mass
under the requested hypothesis.
}
\examples{
d <- as_lr_dist(data.frame(
  log10_lr = c(-1, 0, 2),
  p_h1 = c(0.1, 0.3, 0.6),
  p_h2 = c(0.6, 0.3, 0.1)
))
quantile(d)
quantile(d, probs = c(0.05, 0.95), under_h1 = FALSE)
}
\seealso{
\code{\link[=lr_distribution]{lr_distribution()}}, \code{\link[=summary.lr_dist]{summary.lr_dist()}}, \code{\link[=plot.lr_dist]{plot.lr_dist()}}
}
