% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/belief_trajectory.R
\name{kl_divergence_log10}
\alias{kl_divergence_log10}
\title{Kullback-Leibler divergence in base-10 (bans)}
\usage{
kl_divergence_log10(p, q, epsilon = 1e-20)
}
\arguments{
\item{p}{Numeric vector. Target distribution.}

\item{q}{Numeric vector. Reference distribution (same length as \code{p}).}

\item{epsilon}{Numeric scalar. Small positive constant to avoid
\eqn{\log(0)}. Default \eqn{10^{-20}}.}
}
\value{
Numeric scalar. \eqn{D_{\mathrm{KL}}(p \| q) = \sum_i p_i
  \log_{10}(p_i / q_i)} in bans.
}
\description{
Computes the Kullback-Leibler divergence \eqn{D_{\mathrm{KL}}(p \| q)}
between two distributions, in base-10 logarithm so the result is
expressed in \emph{bans}.
}
\examples{
p <- c(0.7, 0.3)
q <- c(0.5, 0.5)
kl_divergence_log10(p, q)          # positive
kl_divergence_log10(p, p)          # 0

}
\seealso{
\code{\link{entropy_log10}},
\code{\link{trajectory_metrics}}.
}
