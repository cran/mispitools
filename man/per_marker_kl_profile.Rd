% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/per_marker_kl.R
\name{per_marker_kl_profile}
\alias{per_marker_kl_profile}
\title{Per-marker bidirectional KL across a marker profile}
\usage{
per_marker_kl_profile(models, poi = NULL)
}
\arguments{
\item{models}{A \code{list} of \code{marker_model} objects. Names of the list, when
present and non-empty, override the per-model \code{marker_id} in the output
\code{marker} column.}

\item{poi}{Optional character scalar applied to every model, or \code{NULL}
(default) for per-model resolution. Pass a vector by mapping the loop
yourself if you need heterogeneous POIs.}
}
\value{
A \code{data.frame} with the same columns as \code{\link[=per_marker_kl]{per_marker_kl()}} and
\code{nrow(out) == length(models)}. When \code{poi} is a scalar, attribute
\code{"poi"} carries that value; otherwise the resolved POIs appear as
attribute \code{"poi"} of length \code{length(models)}.
}
\description{
Vectorised wrapper around \code{\link[=per_marker_kl]{per_marker_kl()}} for a list of \code{marker_model}
objects. Returns a \code{data.frame} with one row per input model in input
order. When the profile shares one pedigree topology, uses no linkage,
and every mutation model is wired to the C++ backend
(\code{none} / \code{equal} / \code{stepwise}), evaluation routes through a single
cross-marker C++ batch call with a shared mutation-matrix cache;
otherwise it falls back to a per-marker R-level loop.

All models must share the same pedigree topology if \code{poi} is supplied as
a scalar; otherwise the POI is resolved independently for each model.
}
\examples{
if (requireNamespace("pedtools", quietly = TRUE)) {
  ped <- pedtools::nuclearPed(1)
  models <- list(
    M1 = marker_model(ped, "M1", c("a" = 0.4, "b" = 0.6),
                      mutation = list(model = "equal", rate = 1e-3)),
    M2 = marker_model(ped, "M2", c("a" = 0.2, "b" = 0.3, "c" = 0.5),
                      mutation = list(model = "equal", rate = 1e-3))
  )
  per_marker_kl_profile(models)
}
}
\seealso{
\code{\link[=per_marker_kl]{per_marker_kl()}}.
}
