% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nongenetic_feature.R
\name{nongenetic_feature}
\alias{nongenetic_feature}
\title{Non-genetic feature model (S3)}
\usage{
nongenetic_feature(
  type,
  observed,
  model = NULL,
  db_or_freqs = NULL,
  error = NULL,
  tol = 1e-06
)
}
\arguments{
\item{type}{Character scalar. One of \code{"sex"}, \code{"age"}, \code{"region"},
\code{"hair"}, \code{"eyes"}, \code{"pigmentation"}, \code{"birthdate"}, \code{"custom"}.}

\item{observed}{The recorded observation for the unidentified person.
For categorical features a length-one value matching a category of
\code{db_or_freqs} (numeric labels are matched as characters). For \code{age}
a single finite numeric. For \code{birthdate} either a single \code{Date} / a
\code{"YYYY-MM-DD"} string, or a single finite numeric day discrepancy.}

\item{model}{Either \code{NULL} (a type-appropriate default is filled in) or
a named list describing the reference/observation sub-model. For
categorical features the field \code{reference} is one of \code{"marginal"}
(use \code{db_or_freqs} as \code{H2}, the default) or \code{"uniform"}. For \code{age},
\code{reference} is \code{"uniform"} (with a length-2 increasing numeric
\code{range}, default \code{c(1, 80)}) or \code{"empirical"}. For \code{birthdate},
\code{search} is \code{"open"} (default) or \code{"closed"}, with a strictly
increasing numeric \code{cuts} vector (default
\code{c(-120, -30, 30, 120, 240, 360)}). For \code{"custom"}, \code{model} must be a
named list with a character \code{class} field in
\code{c("categorical", "continuous", "date")}.}

\item{db_or_freqs}{The population reference. Categorical: a named numeric
vector (>= 2 categories, entries in \verb{[0, 1]}, summing to 1 within
\code{tol}). Continuous: \code{NULL} when \code{reference = "uniform"}, otherwise a
numeric sample (length >= 2, finite). Date: \code{NULL} for an open search,
otherwise a non-negative numeric vector of bin frequencies of length
\code{length(cuts) + 1} or a \code{data.frame} of declared dates.}

\item{error}{The observation-error model under \code{H1}. Categorical: a
scalar in \verb{[0, 1)} (symmetric misclassification) or a square
row-stochastic numeric matrix whose dimension equals the number of
categories. The matrix is positional: row \code{i} / column \code{j} are the
\code{i}-th / \code{j}-th category in the order of \code{db_or_freqs} (any dimnames
are informational only). Continuous: a scalar in \verb{[0, 1)}. Date: a
numeric
Dirichlet \code{alpha} vector, all strictly positive, of length
\code{length(cuts) + 1}. Defaults are class-appropriate except for
\code{"custom"}, where \code{error} is required.}

\item{tol}{Numeric tolerance for the sum-to-one and row-stochastic
checks. Defaults to \code{1e-6}.}
}
\value{
An object of class \code{"nongenetic_feature"}: a list with
components \code{type}, \code{feature_class}, \code{observed}, \code{model}, \code{error},
\code{db_or_freqs}, and \code{categories} (the category labels for categorical
features, \code{NULL} otherwise).
}
\description{
Constructor for a single non-genetic forensic feature (sex, age, region,
hair colour, eye colour, pigmentation, birth date, or a user-defined
\code{"custom"} feature). It is the non-genetic counterpart of
\code{\link[=marker_model]{marker_model()}}: it bundles the recorded observation, the
reference/observation sub-model, the population reference, and the
observation-error model into a single object that the per-feature engines
(arriving in milestones F6.3--F6.5) consume.

Like \code{\link[=marker_model]{marker_model()}}, this constructor only validates and stores its
inputs; no conditional probability table is built here. The point of F6.1
is to fix the structural contract so that the genetic and non-genetic
paths are symmetric: every non-genetic feature reduces to a CPT under
\code{H1} (missing person, with observation error) and a CPT under \code{H2}
(population marginal), then \code{LR = P(D | H1) / P(D | H2)} --- exactly the
shape of the per-marker genetic path.
}
\section{Feature classes}{

Each \code{type} maps to one of three internal \emph{feature classes} that
determine how \code{db_or_freqs} and \code{error} are interpreted:
\describe{
\item{\code{categorical}}{\code{sex}, \code{region}, \code{hair}, \code{eyes}, \code{pigmentation}.
\code{db_or_freqs} is a named numeric vector of population category
frequencies (the \code{H2} marginal); \code{error} is either a scalar symmetric
misclassification rate or a full row-stochastic confusion matrix
\code{E} with \code{E[true, observed]}. Discrete support, so the categorical
KL is identical to the genetic engine.}
\item{\code{continuous}}{\code{age}. The reference is either \code{"uniform"} over a
numeric \code{range} or \code{"empirical"} from a sample passed in
\code{db_or_freqs}; \code{error} is a scalar mis-binning rate.}
\item{\code{date}}{\code{birthdate}. A Dirichlet model over signed
declared-minus-actual day discrepancy bins (\code{cuts}); \code{error} is the
Dirichlet \code{alpha} vector. \code{search = "open"} uses a uniform \code{H2};
\code{search = "closed"} uses database bin frequencies.}
}
\code{type = "custom"} requires \code{model} to declare its \code{class} (one of the
three above); validation then follows that class.
}

\examples{
# Categorical: biological sex, missing person female, 5\% error
f_sex <- nongenetic_feature(
  type = "sex",
  observed = "F",
  db_or_freqs = c(F = 0.5, M = 0.5),
  error = 0.05
)
print(f_sex)

# Categorical hair colour with a full confusion matrix
E <- error_matrix_hair()
f_hair <- nongenetic_feature(
  type = "hair",
  observed = 1,
  db_or_freqs = c("1" = 0.3, "2" = 0.2, "3" = 0.25, "4" = 0.15, "5" = 0.1),
  error = E
)

# Continuous: age, uniform reference over [1, 80]
f_age <- nongenetic_feature(
  type = "age",
  observed = 42,
  model = list(reference = "uniform", range = c(1, 80)),
  error = 0.05
)

# Date: birth-date discrepancy, open search, default Dirichlet
f_bd <- nongenetic_feature(
  type = "birthdate",
  observed = 45,
  error = c(1, 4, 60, 11, 6, 4, 4)
)
}
\references{
Marsico FL, et al. (2023). "Likelihood ratios for non-genetic evidence
in missing person cases." \emph{Forensic Science International: Genetics},
66, 102891. \doi{10.1016/j.fsigen.2023.102891}
}
\seealso{
\code{\link[=marker_model]{marker_model()}} for the genetic counterpart; the legacy
non-genetic functions \code{\link[=lr_sex]{lr_sex()}}, \code{\link[=lr_age]{lr_age()}}, \code{\link[=lr_hair_color]{lr_hair_color()}},
\code{\link[=lr_birthdate]{lr_birthdate()}}, \code{\link[=lr_pigmentation]{lr_pigmentation()}} whose behaviour this framework
generalises and which serve as its regression oracles.
}
