% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/per_marker_kl.R
\name{per_marker_kl}
\alias{per_marker_kl}
\title{Per-marker bidirectional Kullback-Leibler divergence and expected log10 LR}
\usage{
per_marker_kl(model, poi = NULL)
}
\arguments{
\item{model}{A \code{marker_model} object (see \code{\link[=marker_model]{marker_model()}}).}

\item{poi}{Optional character scalar naming the person of interest in the
pedigree. When \code{NULL} (default), the engine resolves the POI by picking
the last untyped non-founder, falling back to the last non-founder, and
finally to the last member.}
}
\value{
A one-row \code{data.frame} with columns
\describe{
\item{marker}{Marker identifier (\code{character}).}
\item{e_log10_lr_h1}{Expected \code{log10(LR)} under H1.}
\item{e_log10_lr_h2}{Expected \code{log10(LR)} under H2.}
\item{kl_h1_to_h2}{KL(H1 || H2) in nats.}
\item{kl_h2_to_h1}{KL(H2 || H1) in nats.}
\item{abs_cont_violations_h1}{Number of joint states with positive H2
mass but zero H1 mass (integer).}
\item{abs_cont_violations_h2}{Number of joint states with positive H1
mass but zero H2 mass (integer).}
\item{mass_violations_h1}{Total H2 mass on states violating H1 absolute
continuity (numeric).}
\item{mass_violations_h2}{Total H1 mass on states violating H2 absolute
continuity (numeric).}
}
Attribute \code{"poi"} records the resolved POI identifier.
}
\description{
Computes, for a single marker model, the expected \code{log10} likelihood ratio
under both hypotheses and the bidirectional Kullback-Leibler divergence
between the joint H1 and H2 distributions over pedigree typings. Drives the
C++ engine introduced in F2 / F3.1; returns the same numerical content as
the internal reference engine to bit-equivalent precision.

Boundary convention follows the reference engine:
\itemize{
\item \code{P_H1(g) > 0} and \code{P_H2(g) = 0} contribute \code{+Inf} to
\code{kl_h1_to_h2} and \code{e_log10_lr_h1}.
\item \code{P_H1(g) = 0} and \code{P_H2(g) > 0} contribute \code{+Inf} to
\code{kl_h2_to_h1} and \code{-Inf} to \code{e_log10_lr_h2}.
\item States with \code{P_H1 = P_H2 = 0} are skipped (already filtered by the
joint engine).
}
Under \code{mutation = "none"} the H2 distribution typically spans
Mendelian-incompatible states with zero mass under H1, so
\code{kl_h2_to_h1 = +Inf}. The \verb{abs_cont_violations_*} and
\verb{mass_violations_*} columns quantify how much probability mass falls on
violating states in each direction.
}
\examples{
if (requireNamespace("pedtools", quietly = TRUE)) {
  ped <- pedtools::nuclearPed(1)
  freqs <- c("a" = 0.3, "b" = 0.5, "c" = 0.2)
  mm <- marker_model(ped, "M1", freqs,
                     mutation = list(model = "equal", rate = 0.005))
  per_marker_kl(mm)
}
}
\seealso{
\code{\link[=marker_model]{marker_model()}}, \code{\link[=per_marker_kl_profile]{per_marker_kl_profile()}}.
}
