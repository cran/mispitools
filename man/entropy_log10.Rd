% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/belief_trajectory.R
\name{entropy_log10}
\alias{entropy_log10}
\title{Shannon entropy in base-10 (bans)}
\usage{
entropy_log10(p, epsilon = 1e-20)
}
\arguments{
\item{p}{Numeric vector. Probability distribution (must sum to 1, entries
in \eqn{[0, 1]}).}

\item{epsilon}{Numeric scalar. Small positive constant to avoid
\eqn{\log(0)} for zero-probability outcomes. Default \eqn{10^{-20}}.}
}
\value{
Numeric scalar. Shannon entropy \eqn{H(p) = -\sum_i p_i
  \log_{10} p_i} in bans.
}
\description{
Computes the Shannon entropy of a probability distribution, using
base-10 logarithm so the result is expressed in \emph{bans}
(the forensic convention for weight-of-evidence units).
}
\examples{
entropy_log10(c(0.5, 0.5))          # log10(2)
entropy_log10(rep(1/12, 12))        # log10(12)
entropy_log10(c(1, 0, 0, 0))        # ~0

}
\seealso{
\code{\link{kl_divergence_log10}},
\code{\link{trajectory_metrics}}.
}
