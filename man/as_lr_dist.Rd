% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr_dist_s3.R
\name{as_lr_dist}
\alias{as_lr_dist}
\title{Coerce to a Likelihood-Ratio Distribution Object}
\usage{
as_lr_dist(x)
}
\arguments{
\item{x}{A data.frame or list with numeric components \code{log10_lr},
\code{p_h1} and \code{p_h2}, all of the same length. Typically the output of
the per-marker LR distribution engine.}
}
\value{
An object of class \code{lr_dist} (a data.frame with the three
columns), suitable for \code{\link[=summary.lr_dist]{summary.lr_dist()}} and \code{\link[=plot.lr_dist]{plot.lr_dist()}}.
}
\description{
Wraps a sparse \eqn{(\log_{10}\mathrm{LR}, P(\cdot\mid H_1),
P(\cdot\mid H_2))} table in an \code{lr_dist} object so the decision-
theoretic \code{summary()} and \code{plot()} methods can be applied. This is
the low-level constructor; the model-aware \code{lr_distribution()}
wrapper is added in a later release and produces the same class.
}
\details{
Each row is an atom of the discrete LR distribution: \code{log10_lr} is
the log10 likelihood ratio, \code{p_h1} its probability under \eqn{H_1}
(the person of interest is the missing person) and \code{p_h2} its
probability under \eqn{H_2} (unrelated). \code{+Inf} / \code{-Inf} atoms
(which arise under \code{mutation = "none"}) are allowed and handled by
the summaries with the limit \eqn{0\log 0 = 0}.
}
\examples{
d <- data.frame(
  log10_lr = c(-1, 0, 2),
  p_h1 = c(0.1, 0.3, 0.6),
  p_h2 = c(0.6, 0.3, 0.1)
)
x <- as_lr_dist(d)
summary(x)
}
\seealso{
\code{\link[=summary.lr_dist]{summary.lr_dist()}}, \code{\link[=plot.lr_dist]{plot.lr_dist()}}
}
