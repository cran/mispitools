% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker_model.R
\name{marker_model}
\alias{marker_model}
\title{Marker model (S3)}
\usage{
marker_model(
  ped,
  marker_id,
  freqs,
  mutation = list(model = "none", rate = 0),
  linkage = NULL,
  tol = 1e-06
)
}
\arguments{
\item{ped}{A \code{pedtools::ped} object describing the pedigree. Singletons
and \code{pedList} objects are not accepted at this stage.}

\item{marker_id}{Character scalar. Identifier used to label the marker
(e.g. \code{"D3S1358"}). When \code{ped} already carries a marker with this
name, downstream engines will read the genotypes of typed individuals
from it; otherwise the model represents the prospective situation
prior to typing.}

\item{freqs}{Named numeric vector of population allele frequencies. Names
are allele labels, values must lie in \verb{[0, 1]} and sum to 1 within
\code{tol}. At least two alleles are required.}

\item{mutation}{List describing the mutation model. Required field
\code{model} is one of \code{"none"}, \code{"equal"}, \code{"stepwise"}, \code{"asymmetric"}.
For \code{"none"} no further fields are needed; for the other models a
numeric \code{rate} in \verb{[0, 1)} is required. \code{"stepwise"} additionally
accepts \code{ratio} (geometric step ratio in \verb{(0, 1)}); \code{"asymmetric"}
additionally accepts \code{ratio} and \code{bias} (\code{u} parameter, in \verb{[0, 1]}).
Defaults to \code{list(model = "none", rate = 0)}.}

\item{linkage}{Either \code{NULL} (default; unlinked marker) or a list with
fields \code{partner} (character, the marker identifier of the linked
partner) and \code{theta} (recombination fraction in \verb{[0, 0.5]}). Linkage
support is wired in milestone F5; F1 only validates the structure.}

\item{tol}{Numeric tolerance used when checking that \code{freqs} sums to 1.
Defaults to \code{1e-6}.}
}
\value{
An object of class \code{"marker_model"} carrying the validated
inputs as components \code{ped}, \code{marker_id}, \code{freqs}, \code{mutation},
\code{linkage}, and \code{alleles} (\code{names(freqs)}).
}
\description{
Constructor for a single-marker forensic genetic model. Bundles the
pedigree, the marker identifier, the population allele frequencies, the
mutation model, and (optionally) a linkage descriptor. The resulting
object is the unit consumed by the per-marker engines (\code{per_marker_kl},
\code{per_marker_lr_dist}) that arrive in later milestones.

This is the public entry point for the F1 reference engine. The
constructor only validates and stores its inputs; the actual joint CPT
construction is done downstream.
}
\examples{
if (requireNamespace("pedtools", quietly = TRUE)) {
  ped <- pedtools::nuclearPed(1)
  freqs <- c("12" = 0.2, "13" = 0.3, "14" = 0.5)
  mm <- marker_model(
    ped = ped,
    marker_id = "M1",
    freqs = freqs,
    mutation = list(model = "equal", rate = 1e-3)
  )
  print(mm)
}
}
